First run the install requirements

To comple program:
```bash
mkdir build && cd build
cmake ..
make -j7
```

To run your program replace the text in quotes (no quotes)
```bash
./"your_executable_name"
```
